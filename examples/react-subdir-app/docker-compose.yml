services:
  react-subdir-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-subdir-app
    networks:
      - gateway-network
    restart: unless-stopped

networks:
  gateway-network:
    external: true
