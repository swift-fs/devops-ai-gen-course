services:
  vue-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vue-app
    networks:
      - gateway-network
    restart: unless-stopped

networks:
  gateway-network:
    external: true
