services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-app
    networks:
      - gateway-network
    restart: unless-stopped

networks:
  gateway-network:
    external: true
